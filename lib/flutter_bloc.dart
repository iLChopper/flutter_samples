import 'package:flutter/material.dart';

class FlutterBloc extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Flutter Bloc Page '),
      ),
    );
  }
}
